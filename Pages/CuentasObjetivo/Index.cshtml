@page
@model Progra1_Bases.Pages.CuentasObjetivo.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CuentaObjetivo[0].Saldo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuentaObjetivo[0].FechaInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuentaObjetivo[0].FechaFinalizacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuentaObjetivo[0].Monto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuentaObjetivo[0].Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CuentaObjetivo) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Saldo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinalizacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Monto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
