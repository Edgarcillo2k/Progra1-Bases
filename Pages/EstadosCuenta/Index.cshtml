@page
@model Progra1_Bases.Pages.EstadosCuenta.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuenta[0].FechaInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuenta[0].FechaFinalizacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuenta[0].SaldoInicial)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuenta[0].SaldoFinal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuenta[0].CantRetirosCajeroHumano)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuenta[0].CantRetirosCajeroAutomatico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuenta[0].SaldoMinimo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EstadoCuenta) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinalizacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaldoInicial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaldoFinal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantRetirosCajeroHumano)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantRetirosCajeroAutomatico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaldoMinimo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
